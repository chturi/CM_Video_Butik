@model IEnumerable<CM_Video_Butik.Models.MovieModels>

@{
    ViewBag.Title = "Index";
}

<h2>Movies In Store</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Genre)
        </th>
        <th>
            Quantity
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Genre)
        </td>
        <td>
            @(item.QuantityTotalStock-item.QuantityRented)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Title }) |
            @Html.ActionLink("Details", "Details", new { id=item.Title }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Title })
        </td>
    </tr>
}

</table>
